# legends-obs-kit — Claude Code

Skill: `legends-obs-kit`. Resolve the CLI through the skill, then run `doctor --pretty`, `manifest --pretty`, and `agent:context --pretty` before operating. In a cloned or extracted release, use `node .\dist\index.js <command>`. Use only the guarded CLI; no ambient MCP or raw secret/config dumps. Writes require live mode plus `--confirm` and must close with readback and receipt verification.
