# Legends OBS Kit — Gemini CLI

Use skill `legends-obs-kit`. Doctor first. Operate the TypeScript CLI, preserve dry-run by default, never expose OBS credentials, and require the canary before claiming HDR recording works.
